diff --git a/node_modules/metrixjs-wallet/lib/Insight.d.ts b/node_modules/metrixjs-wallet/lib/Insight.d.ts
index eeebc2a..3be29f2 100644
--- a/node_modules/metrixjs-wallet/lib/Insight.d.ts
+++ b/node_modules/metrixjs-wallet/lib/Insight.d.ts
@@ -3,6 +3,8 @@ import { INetworkInfo } from "./Network";
 export declare class Insight {
     private baseURL;
     static forNetwork(network: INetworkInfo): Insight;
+    static setBaseURLOverride(networkName: string, baseURL: string): void;
+    static clearBaseURLOverride(networkName: string): void;
     private fetchApi;
     constructor(baseURL: string);
     static toSatoshi(amount: number | Big): number | Big;
diff --git a/node_modules/metrixjs-wallet/lib/Insight.js b/node_modules/metrixjs-wallet/lib/Insight.js
index 3e4ef64..681bbf4 100644
--- a/node_modules/metrixjs-wallet/lib/Insight.js
+++ b/node_modules/metrixjs-wallet/lib/Insight.js
@@ -18,6 +18,7 @@ const INSIGHT_BASEURLS = {
     [constants_1.NetworkNames.TESTNET]: "https://testnet-explorer.metrixcoin.com/api",
     [constants_1.NetworkNames.REGTEST]: "http://localhost:3001/api",
 };
+const INSIGHT_BASEURL_OVERRIDES = {};
 class Insight {
     constructor(baseURL) {
         this.baseURL = baseURL;
@@ -29,8 +30,14 @@ class Insight {
     // public static testnet(): Insight {
     //   return new Insight(TESTNET_API_BASEURL)
     // }
+    static setBaseURLOverride(networkName, baseURL) {
+        INSIGHT_BASEURL_OVERRIDES[networkName] = baseURL;
+    }
+    static clearBaseURLOverride(networkName) {
+        delete INSIGHT_BASEURL_OVERRIDES[networkName];
+    }
     static forNetwork(network) {
-        const baseURL = INSIGHT_BASEURLS[network.name];
+        const baseURL = INSIGHT_BASEURL_OVERRIDES[network.name] || INSIGHT_BASEURLS[network.name];
         if (baseURL == null) {
             throw new Error(`No Insight API defined for network: ${network.name}`);
         }
@@ -122,7 +129,10 @@ class Insight {
             if (res.status === 0) {
                 return { txid: res.id };
             }
-            return res;
+            // The API responds 200 OK even when the node rejects the transaction (e.g. bad
+            // inputs, insufficient fee) -- without this, a rejection was silently returned as
+            // if it were a successful result, with no thrown error anywhere to report it.
+            throw new Error(res.message || 'Failed to broadcast transaction.');
         });
     }
     GetRawTx(tx) {
@@ -177,7 +187,12 @@ class Insight {
             if (feeRate < 0) {
                 return feeRate;
             }
-            return Math.ceil(feeRate / 1024);
+            // Bitcoin-protocol fee rates (and this node's own minRelayTxFee enforcement) are
+            // denominated per 1000 bytes (decimal kB), not 1024 (KiB) -- using 1024 here
+            // systematically under-estimated the required per-byte rate by ~2.4%, which was
+            // enough on its own to land transactions just under the network's minimum relay
+            // fee and get them rejected.
+            return Math.ceil(feeRate / 1000);
         });
     }
     /**
diff --git a/node_modules/metrixjs-wallet/lib/Wallet.d.ts b/node_modules/metrixjs-wallet/lib/Wallet.d.ts
index 713da02..4224bd4 100644
--- a/node_modules/metrixjs-wallet/lib/Wallet.d.ts
+++ b/node_modules/metrixjs-wallet/lib/Wallet.d.ts
@@ -2,7 +2,7 @@ import { ECPairInterface } from 'bitcoinjs-lib';
 import { BIP32Interface } from "bip32";
 import { INetworkInfo } from "./Network";
 import { Insight } from "./Insight";
-import { IUTXO, IContractSendTXOptions, ISendTxOptions, IContractCreateTXOptions } from "./tx";
+import { IUTXO, IContractSendTXOptions, ISendTxOptions, IContractCreateTXOptions, ITxOutput } from "./tx";
 import { IScryptParams } from "./scrypt";
 export declare class Wallet {
     keyPair: ECPairInterface;
@@ -35,13 +35,14 @@ export declare class Wallet {
     /**
      * Generate and sign a payment transaction.
      *
-     * @param to The receiving address
-     * @param amount The amount to transfer (in satoshi)
+     * @param to The receiving address, or an array of {address, amount} outputs for a
+     *   multi-recipient transaction (in which case `amount` is ignored).
+     * @param amount The amount to transfer (in satoshi). Ignored when `to` is an array.
      * @param opts
      *
      * @returns The raw transaction as hexadecimal string
      */
-    generateTx(to: string, amount: number, opts?: ISendTxOptions): Promise<string>;
+    generateTx(to: string | ITxOutput[], amount: number | undefined, opts?: ISendTxOptions): Promise<string>;
     /**
      * Estimate the maximum value that could be sent from this wallet address.
      *
@@ -56,12 +57,13 @@ export declare class Wallet {
      * using the wallet's private key, and the raw transaction submitted to a
      * remote API (without revealing the wallet's secret).
      *
-     * @param to The receiving address
-     * @param amount The amount to transfer (in satoshi)
+     * @param to The receiving address, or an array of {address, amount} outputs for a
+     *   multi-recipient transaction (in which case `amount` is ignored).
+     * @param amount The amount to transfer (in satoshi). Ignored when `to` is an array.
      * @param opts
      * @return The raw transaction as hexadecimal string
      */
-    send(to: string, amount: number, opts?: ISendTxOptions): Promise<Insight.ISendRawTxResult>;
+    send(to: string | ITxOutput[], amount: number | undefined, opts?: ISendTxOptions): Promise<Insight.ISendRawTxResult>;
     /**
      * Submit a signed raw transaction to the network.
      *
diff --git a/node_modules/metrixjs-wallet/lib/Wallet.js b/node_modules/metrixjs-wallet/lib/Wallet.js
index b2b34f4..45590e7 100644
--- a/node_modules/metrixjs-wallet/lib/Wallet.js
+++ b/node_modules/metrixjs-wallet/lib/Wallet.js
@@ -40,12 +40,13 @@ const bip32_1 = __importDefault(require("bip32"));
 const Insight_1 = require("./Insight");
 const tx_1 = require("./tx");
 const scrypt_1 = require("./scrypt");
+const buffer_1 = require("buffer");
 /**
  * The default relay fee rate (per byte) if network doesn't cannot estimate how much to use.
  *
  * This value will be used for testnet.
  */
-const defaultTxFeePerByte = Math.ceil((10 * 1e8) / 1024);
+const defaultTxFeePerByte = Math.ceil((10 * 1e8) / 1000);
 const bip32 = bip32_1.default(ecc);
 class Wallet {
     constructor(keyPair, network) {
@@ -231,7 +232,24 @@ class Wallet {
             // could cause overlapping UXTOs to be used.
             // FIXME: make the two compatible...
             // massage UXTO to format accepted by bitcoinjs
-            const bitcoinjsUTXOs = uxtos.map((uxto) => (Object.assign(Object.assign({}, uxto), { pos: uxto.vout, value: uxto.satoshis, hash: uxto.txid }))).filter((utxo) => utxo.confirmations >= 960 || !utxo.isStake);
+            const bitcoinjsUTXOs = uxtos.map((uxto) => (Object.assign(Object.assign({}, uxto), { pos: uxto.vout, value: uxto.satoshis, hash: uxto.txid }))).filter((utxo) => utxo.confirmations >= 960 || !utxo.isStake).filter((utxo) => {
+                // Defends against a bad/corrupt `rawtx` from the API (would otherwise crash deep inside
+                // bitcoinjs-lib with a cryptic "value out of range" once this UTXO is selected for spending).
+                // Cross-checks the parsed output's value against the UTXO's reported value as a sanity check.
+                try {
+                    const parsedTx = bitcoin.Transaction.fromBuffer(buffer_1.Buffer.from(utxo.rawtx, 'hex'));
+                    const parsedOut = parsedTx.outs[utxo.vout];
+                    if (!parsedOut || parsedOut.value !== utxo.satoshis) {
+                        console.error('getBitcoinjsUTXOs: excluding UTXO with mismatched rawtx', utxo.txid, utxo.vout);
+                        return false;
+                    }
+                    return true;
+                }
+                catch (err) {
+                    console.error('getBitcoinjsUTXOs: excluding UTXO with unparseable rawtx', utxo.txid, utxo.vout, err);
+                    return false;
+                }
+            });
             return bitcoinjsUTXOs;
         });
     }
diff --git a/node_modules/metrixjs-wallet/lib/tx.d.ts b/node_modules/metrixjs-wallet/lib/tx.d.ts
index 428b0d3..66292cf 100644
--- a/node_modules/metrixjs-wallet/lib/tx.d.ts
+++ b/node_modules/metrixjs-wallet/lib/tx.d.ts
@@ -55,17 +55,37 @@ export interface IUTXO {
     isStake: boolean;
     confirmations: number;
 }
+export interface ISelectTxsResult {
+    inputs: IUTXO[];
+    feeTotal: number;
+}
+export interface ITxOutput {
+    address: string;
+    /**
+     * unit: satoshi
+     */
+    amount: number;
+}
+/**
+ * Select UTXOs to cover amount + fee (satoshi/byte), reusing the exact accounting the
+ * transaction builders use -- lets callers get a real fee estimate without signing/building
+ * a transaction.
+ * @param numOutputs Worst-case number of outputs the built transaction will have (destinations
+ *   + change), used to size the reserved fee. Defaults to 2 (one destination + one change).
+ */
+export declare function selectTxs(unspentTransactions: IUTXO[], amount: number, fee: number, numOutputs?: number): ISelectTxsResult;
 export declare function estimatePubKeyHashTransactionMaxSend(utxos: IUTXO[], to: string, feeRate: number): number;
 /**
  * Build a pay-to-pubkey-hash transaction
  *
  * @param keyPair
- * @param to
- * @param amount (unit: satoshi)
+ * @param to A single receiving address, or an array of {address, amount} outputs for a
+ *   multi-recipient transaction (in which case `amount` is ignored).
+ * @param amount (unit: satoshi) Ignored when `to` is an array.
  * @param feeRate
  * @param utxoList
  */
-export declare function buildPubKeyHashTransaction(utxos: IUTXO[], keyPair: ECPairInterface, to: string, amount: number, feeRate: number): string;
+export declare function buildPubKeyHashTransaction(utxos: IUTXO[], keyPair: ECPairInterface, to: string | ITxOutput[], amount: number | undefined, feeRate: number): string;
 /**
  * Build a create-contract transaction
  *
diff --git a/node_modules/metrixjs-wallet/lib/tx.js b/node_modules/metrixjs-wallet/lib/tx.js
index 49bcaae..2f7e632 100644
--- a/node_modules/metrixjs-wallet/lib/tx.js
+++ b/node_modules/metrixjs-wallet/lib/tx.js
@@ -32,6 +32,20 @@ function ensureAmountInteger(n) {
         throw new Error(`Expect tx amount to be an integer, got: ${n}`);
     }
 }
+// Standard P2PKH transaction size approximations (bytes) -- the same figures
+// Bitcoin Core and most Bitcoin-like wallets use for legacy (non-segwit) txs.
+const TX_BASE_BYTES = 10;
+const TX_INPUT_BYTES = 148;
+const TX_OUTPUT_BYTES = 34;
+function estimateTxBytes(numInputs, numOutputs) {
+    return TX_BASE_BYTES + (numInputs * TX_INPUT_BYTES) + (numOutputs * TX_OUTPUT_BYTES);
+}
+// An output is "dust" if the cost to later spend it (one more P2PKH input)
+// would exceed 1/3 of its own value at the current fee rate -- Bitcoin Core's
+// standard definition.
+function dustThreshold(feeRate) {
+    return Math.ceil(3 * feeRate * TX_INPUT_BYTES);
+}
 function estimatePubKeyHashTransactionMaxSend(utxos, to, feeRate) {
     let maxAmount = 0;
     for (const utxo of utxos) {
@@ -57,7 +71,7 @@ exports.estimatePubKeyHashTransactionMaxSend = estimatePubKeyHashTransactionMaxS
  * @param Number fee(unit: satoshis)
  * @returns [transaction]
  */
-function selectTxs(unspentTransactions, amount, fee) {
+function selectTxs(unspentTransactions, amount, fee, numOutputs = 2) {
     //sort the utxo
     var matureList = [];
     var immatureList = [];
@@ -80,7 +94,11 @@ function selectTxs(unspentTransactions, amount, fee) {
         var tx = unspentTransactions[i];
         findTotal = findTotal.plus(tx.value);
         find[find.length] = tx;
-        feeTotal = feeTotal.plus(fee);
+        // fee is satoshi/byte; recompute the real fee for the tx size so far
+        // (assumes worst-case numOutputs outputs -- destinations/opcall + change --
+        // which slightly over-reserves when there's no change output, never
+        // under-reserves) instead of treating fee as a flat per-input amount.
+        feeTotal = new bignumber_js_1.BigNumber(Math.ceil(estimateTxBytes(find.length, numOutputs) * fee));
         if (findTotal.isGreaterThanOrEqualTo(value.plus(feeTotal)))
             break;
     }
@@ -89,19 +107,26 @@ function selectTxs(unspentTransactions, amount, fee) {
     }
     return { inputs: find, feeTotal: feeTotal.toNumber() };
 }
+exports.selectTxs = selectTxs;
 /**
  * Build a pay-to-pubkey-hash transaction
  *
  * @param keyPair
- * @param to
- * @param amount (unit: satoshi)
+ * @param to A single receiving address, or an array of {address, amount} outputs for a
+ *   multi-recipient transaction (in which case `amount` is ignored).
+ * @param amount (unit: satoshi) Ignored when `to` is an array.
  * @param feeRate
  * @param utxoList
  */
 function buildPubKeyHashTransaction(utxos, keyPair, to, amount, feeRate) {
-    ensureAmountInteger(amount);
+    const outputs = Array.isArray(to) ? to : [{ address: to, amount }];
+    for (const output of outputs) {
+        ensureAmountInteger(output.amount);
+    }
     const senderAddress = bitcoin.payments.p2pkh({ pubkey: keyPair.publicKey, network: keyPair.network }).address;
-    let { inputs, feeTotal: txfee } = selectTxs(utxos, amount, feeRate);
+    const totalAmount = outputs.reduce((sum, output) => sum + output.amount, 0);
+    // Worst case is every destination output plus a change output.
+    let { inputs, feeTotal: txfee } = selectTxs(utxos, totalAmount, feeRate, outputs.length + 1);
     if (inputs == null) {
         throw new Error("could not find UTXOs to build transaction");
     }
@@ -115,15 +140,20 @@ function buildPubKeyHashTransaction(utxos, keyPair, to, amount, feeRate) {
         });
         vinSum = vinSum.plus(input.value);
     }
-    if (vinSum.isEqualTo(new bignumber_js_1.BigNumber(amount))) {
-        amount = new bignumber_js_1.BigNumber(amount).minus(txfee).toNumber();
+    // "Send max" only makes sense for a single recipient -- ambiguous which
+    // recipient would absorb the fee otherwise.
+    if (outputs.length === 1 && vinSum.isEqualTo(new bignumber_js_1.BigNumber(totalAmount))) {
+        outputs[0].amount = new bignumber_js_1.BigNumber(outputs[0].amount).minus(txfee).toNumber();
+    }
+    const outputTotal = outputs.reduce((sum, output) => sum + output.amount, 0);
+    for (const output of outputs) {
+        txb.addOutput({ address: output.address, value: output.amount });
     }
-    txb.addOutput({ address: to, value: amount });
     const change = vinSum
         .minus(txfee)
-        .minus(amount)
+        .minus(outputTotal)
         .toNumber();
-    if (change > 0) {
+    if (change > dustThreshold(feeRate)) {
         txb.addOutput({ address: senderAddress, value: change });
     }
     for (let i = 0; i < inputs.length; i++) {
@@ -177,7 +207,7 @@ function buildCreateContractTransaction(utxos, keyPair, code, feeRate, opts = {}
         .minus(txfee)
         .minus(gasLimitFee)
         .toNumber();
-    if (change > 0) {
+    if (change > dustThreshold(feeRate)) {
         txb.addOutput({ address: fromAddress, value: change });
     }
     for (let i = 0; i < inputs.length; i++) {
@@ -278,7 +308,7 @@ function buildSendToContractTransaction(utxos, keyPair, contractAddress, encoded
         .minus(gasLimitFee)
         .minus(amount)
         .toNumber();
-    if (change > 0) {
+    if (change > dustThreshold(feeRate)) {
         txb.addOutput({ address: senderAddress, value: change });
     }
     for (let i = 0; i < inputs.length; i++) {
@@ -290,7 +320,7 @@ function buildSendToContractTransaction(utxos, keyPair, contractAddress, encoded
 }
 exports.buildSendToContractTransaction = buildSendToContractTransaction;
 // The prevalent network fee is 10 per KB. If set to 100 times of norm, assume error.
-const MAX_FEE_RATE = Math.ceil((10 * 100 * 1e8) / 1024);
+const MAX_FEE_RATE = Math.ceil((10 * 100 * 1e8) / 1000);
 function checkFeeRate(feeRate) {
     if (feeRate > MAX_FEE_RATE) {
         throw new Error("Excessive tx fees, is set to 100 times of norm.");
